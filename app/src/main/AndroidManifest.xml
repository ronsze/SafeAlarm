<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.myproject.safealarm">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".feature.missing.map.MissingMapActivity"></activity>
        <activity android:name=".feature.guard.each_info.EachInfoActivity" />
        <activity android:name=".SetPassActivty" />
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="fullSensor"
            tools:replace="screenOrientation" />
        <activity android:name=".feature.qrcode.QRCodeActivity" />
        <activity android:name=".AlarmAddActivity" />
        <activity android:name=".RangeAddActivity" />
        <activity android:name=".feature.guard.alarm.AlarmSetActivity" />
        <activity
            android:name=".feature.ward.info.WardInfoActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".RangeSetActivity" />
        <activity android:name=".feature.missing.info.MissingInfoActivity" />
        <activity android:name=".feature.ward.setting.WardSettingActivity" />
        <activity android:name=".WardHelpActivity" />
        <activity android:name=".feature.guard.setting.GuardSettingActivity" />
        <activity
            android:name=".feature.guard.help.GuardHelpActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".feature.guard.map.GuardMapActivity" />

        <service
            android:name=".ForegroundService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="location" />

        <activity android:name=".feature.loading.LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".feature.ward.WardActivity" />
        <activity android:name=".feature.guard.GuardActivity" />
        <activity android:name=".feature.regist.RegistActivity" />

        <receiver android:name=".alarmManagerReceiver" />

        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="5dmemz2dyr" />
    </application>

</manifest>