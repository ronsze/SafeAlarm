<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.myproject.safealarm">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MissingInfoActivity"></activity>
        <activity android:name=".WardSettingActivity" />
        <activity android:name=".WardHelpActivity" />
        <activity android:name=".GuardSettingActivity" />
        <activity android:name=".GuardHelpActivity" />
        <activity
            android:name=".GuardMapActivity"
            android:label="@string/title_activity_guard_map"
            android:theme="@style/NoActionBar" />

        <service
            android:name=".ForegroundService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WardActivity" />
        <activity android:name=".GuardActivity" />
        <activity android:name=".RegistActivity" />
        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="99mlf08z2p" />
    </application>

</manifest>